{
  "scripts": {
    "start": "json-server --watch ./node_endpoints/bb-branches.json --port 5000"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "json-server": "^0.17.0",
    "swagger-parser": "^10.0.3",
    "swagger-yaml": "^1.0.0"
  },
  "devDependencies": {
    "mock-yaml": "^1.0.3"
  }
}
